# Copy this file to `.env` and fill in your credentials for local runs.
OPENAI_API_KEY=sk-your-key-here
# Optional: override the auto-selected workspace folder (absolute path).
# WORKSPACE_ROOT=/Users/you/path/to/opencite_workspace
